 <% if session[:logeado].nil? %>

<script type="text/javascript">
<!--
window.location = "/sessions/new"
//-->
</script>
<% end %>
<%= form_for(@doctor_paciente) do |f| %>
  <% if @doctor_paciente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doctor_paciente.errors.count, "error") %> prohibited this doctor_paciente from being saved:</h2>

      <ul>
      <% @doctor_paciente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nota_paciente %><br />
    <%= f.text_area :nota_paciente %>
  </div>
  <div class="field">
    <%= f.label :fecha_doctorpaciente %><br />
    <%= f.date_select :fecha_doctorpaciente %>
  </div>
  <div class="field">
    <%= f.label :condiciones %><br />
    <%= f.text_area :condiciones %>
  </div>
  
  <div class="field">
    <%= f.label :Cliente %><br />
   	<%= f.collection_select :Cliente_id, Cliente.all, :id, :nombre_cliente, :prompt => true %>
  </div>
  
  
  <div class="field">
    <%= f.label :Doctor %><br />
   	<%= f.collection_select :Doctor_id, Doctor.all, :id, :nombre, :prompt => true %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
